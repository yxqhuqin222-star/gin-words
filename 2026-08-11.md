# 2026-08-11 工作记录

## 欧洲旅游英语单词卡（个人自学站点）
- 目标：六级多年未用、去欧洲旅游，需突击常用词；中英+读音、简单实用。
- 交付：单文件 `index.html`（零依赖），内嵌 151 词 / 11 类，浏览器 Web Speech API 点读（默认 en-GB 回退 en-US），分类筛选+搜索。校验 `test/validate.mjs`（node，词数/分类/en 唯一）。
- 部署：CloudBase 静态托管，环境 `xiaoqin`（体验版，ap-shanghai）。应用 `euro-words`，独立子域名。
  - 公网地址：https://euro-words-xiaoqin-d0g0prppaa09e675e.webapps.tcloudbase.com
  - 部署方式：临时干净目录只含 index.html（避免泄露 .workbuddy/test/），`manageApps(deployApp, serviceName=euro-words, framework=static, installCmd="", buildCmd="")`。
  - 更新：改 index.html 后重新 deployApp 生成新版本，链接不变。
- 验证：curl 返回 HTTP 200，页面内容正确。

## 2026-08-12：接入 CloudBase 数据库（云端进度持久化）
- 新增「已掌握」勾选 + 云端进度同步。后端：PG 表 `word_progress` + 云函数 `wordProgress`（`cloudfunctions/wordProgress/index.js`，invoke 放通匿名）。前端：`index.html` 引入 `@cloudbase/js-sdk@3.7.1` ESM，浏览器 `callFunction(wordProgress)` 读写，`localStorage` uid；头部「已掌握 X / N」+「重置进度」。（详见 .workbuddy/memory/2026-08-12.md）
- 部署：重新 deployApp → 版本 `euro-words-002`，构建 SUCCESS，链接不变。
- 安全域名：`*.webapps.tcloudbase.com`（SYSTEM 默认）已覆盖托管域名，浏览器 callFunction 不受体验版限制。
